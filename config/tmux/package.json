{
  "name": "@rafaeltab-devenv/tmux-config",
  "version": "0.0.0",
  "scripts": {
    "stow": "stow -t ~ -S -d ./src .",
    "perms": "chmod +x ~/.config/tmux/pwd_fromhome.sh",
    "activate": "pnpm run perms && pnpm run stow"
  }
}
