/// Shared PTY reading/writing logic.
#[derive(Debug)]
pub(crate) struct PtyBackend {
    // Internal implementation details
    _private: (),
}

impl PtyBackend {
    pub(crate) fn new() -> Self {
        todo!("Phase 3+: Implement PtyBackend::new")
    }
}
