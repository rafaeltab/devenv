{
  "name": "@rafaeltab-devenv/banner",
  "version": "0.0.0",
  "scripts": {
    "build": "cargo build",
    "build:release": "cargo build --release",
    "dev": "cargo dev",
    "activate": "mkdir -p ~/.local/bin && cp target/release/rbanner ~/.local/bin/",
    "format": "cargo fmt",
    "lint": "cargo build --all-features && cargo clippy --all-features -- -Dwarnings"
  }
}
