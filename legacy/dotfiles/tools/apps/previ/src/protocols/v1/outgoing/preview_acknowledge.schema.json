{
    "type": "object",
    "properties": {
        "protocolVersion": {
            "type": "string",
            "description": "The version of the protocol."
        },
        "messageType": {
            "type": "string",
            "const": "preview_acknowledge",
            "description": "The type of the message."
        },
        "message": {
            "type": "string",
            "description": "A human-readable message indicating the start of the preview process."
        },
        "path": {
            "type": "string",
            "description": "The path to the file that is starting to be previewed."
        }
    },
    "required": ["protocolVersion", "messageType", "message", "path"],
    "additionalProperties": false
}
