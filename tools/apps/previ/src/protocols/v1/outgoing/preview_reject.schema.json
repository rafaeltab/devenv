{
    "type": "object",
    "properties": {
        "protocolVersion": {
            "type": "string",
            "description": "The version of the protocol."
        },
        "messageType": {
            "type": "string",
            "const": "preview_reject",
            "description": "The type of the message."
        },
        "reason": {
            "type": "string",
            "description": "A human-readable message explaining why the preview process was rejected."
        },
        "path": {
            "type": "string",
            "description": "The path of the file that is rejected to be previewed."
        }
    },
    "required": ["protocolVersion", "messageType", "reason", "path"],
    "additionalProperties": false
}
