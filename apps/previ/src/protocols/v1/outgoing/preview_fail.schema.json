{
    "type": "object",
    "properties": {
        "protocolVersion": {
            "type": "string",
            "description": "The version of the protocol."
        },
        "messageType": {
            "type": "string",
            "const": "preview_fail",
            "description": "The type of the message."
        },
        "reason": {
            "type": "string",
            "description": "A human-readable message explaining why the preview process failed."
        },
        "path": {
            "type": "string",
            "description": "The path of the file that failed to be previewed."
        }
    },
    "required": ["protocolVersion", "messageType", "reason", "path"],
    "additionalProperties": false
}
