[package]
name = "tui-test"
version = "0.1.0"
edition = "2021"
description = "Framework-agnostic TUI testing library for terminal applications"

[dependencies]
portable-pty = "0.9"
alacritty_terminal = "0.25"
thiserror = "1"

[dev-dependencies]
crossterm = "0.28"
